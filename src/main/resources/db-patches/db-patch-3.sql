create TABLE Products
(
    id         INT     NOT NULL,
    name       VARCHAR NOT NULL,
    quantity   INT     NOT NULL,
    price      DOUBLE  NOT NULL,
    printPrice DOUBLE  NOT NULL,
    printTime  DOUBLE  NOT NULL
);
